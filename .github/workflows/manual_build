name: manual_build

on: workflow_dispatch

jobs:
  build:
  
    runs-on: windows-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        ref: 'Housekeeping'

      - name Setup MSBuild
        uses: microsoft
