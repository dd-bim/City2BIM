<UserControl x:Class="IFCTerrainGUI.GUI.ElevationGrid.ucElevationGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IFCTerrainGUI.GUI.ElevationGrid"
             mc:Ignorable="d" 
             d:DesignHeight="525" d:DesignWidth="385">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Button x:Name="btnReadGrid" Content="Read 'Grid' file" Click="btnReadGrid_Click"/>
        <Button x:Name="btnProcessGrid" Grid.Row="3" Content="Process 'Grid'" Click="btnProcessGrid_Click" IsEnabled="False"/>
        
        <StackPanel x:Name="stpGridSize" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" IsEnabled="False">
            <TextBlock Text="Grid size:" Margin="0,0,5,0"/>
            <TextBox x:Name="tbGridSize" Width="50" PreviewTextInput="NumberValidationTextBox" TextChanged="tbBB_TextChanged" TextAlignment="Center"/>
            <TextBlock Text="m" Margin="5,0,0,0"/>
        </StackPanel>

        <GroupBox
            Grid.Row="2"
            Header="Bounding Box"
            >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>

                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition/>

                </Grid.RowDefinitions>
                <CheckBox 
                    x:Name="chkGridBB" 
                    Grid.ColumnSpan="2" 
                    Content="enable Bounding Box" Margin="0,5,0,0" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center" 
                    Checked="chkGridBB_Checked" 
                    Unchecked="chkGridBB_Unchecked"
                    IsEnabled="False"/>
                
                <TextBlock Grid.Row="1" Text="North [m]:"   VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Text="East [m]:"    VerticalAlignment="Center"/>
                <TextBlock Grid.Row="3" Text="South [m]:"   VerticalAlignment="Center"/>
                <TextBlock Grid.Row="4" Text="West [m]:"    VerticalAlignment="Center"/>

                <StackPanel x:Name="stpGridBB" Grid.Row="1" Grid.Column="1" Grid.RowSpan="4" IsEnabled="False">
                    <TextBox x:Name="tbBBNorth" Height="20" Margin="0,3,0,0" PreviewTextInput="NumberValidationTextBox" TextChanged="tbBB_TextChanged"/>
                    <TextBox x:Name="tbBBEast" Height="20" Grid.Column="2" Margin="0,5,0,0" PreviewTextInput="NumberValidationTextBox" TextChanged="tbBB_TextChanged"/>
                    <TextBox x:Name="tbBBSouth" Height="20" Grid.Column="2" Margin="0,5,0,0" PreviewTextInput="NumberValidationTextBox" TextChanged="tbBB_TextChanged"/>
                    <TextBox x:Name="tbBBWest" Height="20" Margin="0,5,0,0" PreviewTextInput="NumberValidationTextBox" TextChanged="tbBB_TextChanged"/>
                </StackPanel>
            </Grid>
            
        </GroupBox>
    </Grid>
</UserControl>
